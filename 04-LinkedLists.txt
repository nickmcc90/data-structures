A sequential list of nodes that hold data which point to other
nodes also containing data.

The first node is called a head, the last node is called a tail. Quick additions
and removals are possible.

Doubly linked lists for each node hold a reference to the previous and next nodes.